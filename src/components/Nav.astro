<nav>
  <div class="inner">
    <a href="#hero">首页</a>
    <a href="#works">作品</a>
    <a href="#blog">随笔</a>
    <a href="#about">关于</a>
    <a href="#contact">联系</a>
    <a href="#messages">留言</a>
    <a href="/login" class="login-link">登录</a>
  </div>
</nav>

<style>
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 20;
    background: rgba(27, 42, 58, 0.96);
    backdrop-filter: blur(16px) saturate(1.4);
    font-family: var(--font-body);
    font-size: 12.5px;
    font-weight: 500;
    letter-spacing: 1.5px;
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }
  nav .inner {
    max-width: 780px; margin: 0 auto;
    display: flex; justify-content: space-between;
    padding: 0 28px;
  }
  nav a {
    display: block;
    text-decoration: none;
    color: rgba(255,255,255,0.65);
    padding: 10px 0;
    position: relative;
    transition: color 0.3s;
  }
  nav a::after {
    content: '';
    position: absolute; bottom: 0; left: 50%; transform: translateX(-50%);
    width: 0; height: 2px;
    background: var(--gold-light);
    border-radius: 1px;
    transition: width 0.3s;
  }
  nav a:hover { color: #fff; }
  nav a:hover::after { width: 60%; }
  nav .login-link {
    color: rgba(255,255,255,0.4);
    font-size: 11.5px;
    letter-spacing: 1px;
  }
  nav .login-link:hover { color: rgba(255,255,255,0.8); }
  nav .login-link:hover::after { width: 0; }

  @media (max-width: 640px) {
    nav .inner { padding: 0 14px; }
    nav a { font-size: 11.5px; padding: 9px 0; letter-spacing: 0.8px; }
  }
</style>
