<nav>
  <div class="inner">
    <a href="/">文学社</a>
    <a href="/recommended-works">作品推荐</a>
    <a href="/recommended-authors">作者推荐</a>
    <a href="/events">文学活动</a>
    <a href="/clubs">文学社团</a>
    <span class="user-area">
      <a href="/login">登录</a>
    </span>
  </div>
</nav>

<style>
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 20;
    background: rgba(27, 42, 58, 0.96);
    backdrop-filter: blur(16px) saturate(1.4);
    font-family: var(--font-body);
    font-size: 13px; font-weight: 500;
    letter-spacing: 1.5px;
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }
  nav .inner {
    max-width: 820px; margin: 0 auto;
    display: flex; justify-content: space-between; align-items: center;
    padding: 0 28px;
  }
  nav a {
    display: block; text-decoration: none;
    color: rgba(255,255,255,0.7); padding: 11px 0;
    position: relative; transition: color 0.3s;
  }
  nav a::after {
    content: '';
    position: absolute; bottom: 0; left: 50%; transform: translateX(-50%);
    width: 0; height: 2px;
    background: var(--gold-light); border-radius: 1px;
    transition: width 0.3s;
  }
  nav a:hover { color: #fff; }
  nav a:hover::after { width: 60%; }
  @media (max-width: 640px) {
    nav .inner { padding: 0 14px; }
    nav a { font-size: 11.5px; padding: 9px 0; letter-spacing: 0.8px; }
  }
</style>
